---
title: "Create PRDs"
---

PM handoff isn't only a doc, and it isn't only a screen. From one rough problem, Figr generates the full chain of artifacts a real handoff needs, and keeps them connected on a single canvas.

{/* SCREENSHOT: placeholder so the page renders now. Upload the real file to images/product-design-flow/03-06-create-prds.png then change src to "/images/product-design-flow/03-06-create-prds.png" */}

<img
  alt="PRD, user stories, acceptance criteria, test cases, flow, and prototype connected on the canvas"
  title="PRD chain on the canvas"
  style={{ width:"93%" }}
  lightAlt="PRD, user stories, acceptance criteria, test cases, flow, and prototype connected on the canvas"
  darkAlt="PRD, user stories, acceptance criteria, test cases, flow, and prototype connected on the canvas"
  src="/images/PRD.avif"
  className="dark:hidden"
/>
<img
  alt="PRD, user stories, acceptance criteria, test cases, flow, and prototype connected on the canvas"
  title="PRD chain on the canvas"
  style={{ width:"93%" }}
  lightAlt="PRD, user stories, acceptance criteria, test cases, flow, and prototype connected on the canvas"
  darkAlt="PRD, user stories, acceptance criteria, test cases, flow, and prototype connected on the canvas"
  src="https://placehold.co/1600x900/EEEEEE/31343C?text=03-06-create-prds.png"
  className="hidden dark:block"
/>

{/* @aryan shorten (technical value first, what all type of documents can I make) — actioned: led with the value line above and replaced the prose restatement with the artifact list below */}

From a single problem, Figr can produce:

- PRD and requirements
- User stories and acceptance criteria
- Edge case maps and key states
- Test cases
- User flow

High-fidelity prototype and design direction Start with a rough problem statement. It does not need to be polished. Include:

- What you want to improve
- Who it is for
- Which constraints matter
- Which product context Figr should use Then ask for the first artifact:
  - "Turn this rough product problem into a structured PRD. Use the attached product context, existing screens, design system, analytics notes, and previous decisions, then flag missing requirements and edge cases." From there, generate the rest of the chain: user stories, acceptance criteria, test cases, a user flow, key states, and a prototype. The PRD should not sit alone in a doc. It is the base for your UX work, and the chain stays live: if the flow reveals a missing state, update the PRD; if the prototype exposes a confusing interaction, update the flow.

{/* VIDEO: YouTube hosts this, no upload needed. Replace VIDEO_ID with your video's ID. Delete this block if unused. */}